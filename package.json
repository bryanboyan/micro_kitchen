{
  "name": "css_micro_kitchen",
  "version": "0.0.1",
  "description": "The food ordering and delivery system.",
  "main": "index.js",
  "dependencies": {
    "poisson-process": "^1.0.0"
  },
  "devDependencies": {
    "flow-bin": "^0.93.0",
    "flow-typed": "^2.5.1"
  },
  "scripts": {
    "test": "TODO",
    "flow": "flow",
    "flow:check": "flow check ./src/",
    "flow:build": "flow-remove-types ./src/ -d ./app/ --all --pretty",
    "flow:deps": "flow-typed install"
  },
  "author": "",
  "license": "ISC"
}
