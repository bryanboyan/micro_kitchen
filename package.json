{
  "name": "css_micro_kitchen",
  "version": "0.0.1",
  "description": "The food ordering and delivery system.",
  "main": "app.js",
  "dependencies": {
    "poisson-process": "^1.0.0"
  },
  "devDependencies": {
    "flow-bin": "^0.93.0",
    "flow-typed": "^2.5.1",
    "jest": "^24.1.0",
    "mocha": "^6.0.2"
  },
  "scripts": {
    "test": "jest",
    "flow": "flow",
    "flow:check": "flow check ./src/",
    "build": "flow-remove-types ./src/ -d ./app/ --all --pretty",
    "flow:deps": "flow-typed install"
  },
  "author": "",
  "license": "ISC"
}
